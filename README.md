# gometrics

A lightweight, zero-dependency metrics collection library for Go.

## Install
```bash
go get github.com/eyop23/gometrics
```

## Metric Types

- **Counter** — only goes up (e.g. total requests)
- **Gauge** — goes up or down (e.g. active connections)
- **Timer** — measures durations (e.g. query time)

## Status

🚧 Work in progress
